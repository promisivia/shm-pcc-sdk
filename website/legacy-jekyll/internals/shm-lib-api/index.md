# API Reference

按模块组织 `shm-lib/include/**` 的对外接口，便于“像查 NCCL 一样查 shm-lib”。

如果你只想要完整的“函数签名列表”，直接看 {doc}`func_api`。

```{toctree}
:maxdepth: 2

memory
containers
helpers
queues
comms
occ
stale
help_update
func_api
```
